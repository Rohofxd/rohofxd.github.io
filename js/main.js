UIManager.setupFilterButtons();
UIManager.updateRecipes(SearchEngine.search());

StatTracker.addStatsDOM();

// Initalize PhotoSwipe
var pswpElement = document.querySelectorAll('.pswp')[0];

// var _images = document.getElementsByTagName('img');
// for (let i = 0; i < _images.length; i++) {
//     var _img = _images[i];
//     _img
// }


